[package]
name = "kessler-dashboard"
version = "0.1.0"
edition = "2024"

[dependencies]
axum = "0.8.4"
axum-htmx = { version = "0.8.1", features = ["serde", "tokio", "tower"] }
maud = { version = "0.27.0", features = ["axum"] }
